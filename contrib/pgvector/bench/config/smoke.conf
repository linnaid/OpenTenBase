# Minimal experimental configuration for the entire benchmark

# - Profile name
PROFILE_NAME=smoke

# - Data scale
ROWS=10000
DIMENSIONS=32
QUERY_COUNT=100
RECALL_QUERY_COUNT=100
TOP_K=10

# - IVFFlat parameter matrix
LISTS_VALUES="10 50 100"
PROBES_VALUES="1 2 5 10"

# - Concurrency parameters
CLIENT_VALUES="1"
JOBS=1

# - Query and latency test parmeters
WARMUP_SECONDS=2
DURATION_SECONDS=5
REPEATS=1
# Disable JIT
JIT=off
# Disable sequential scans
ENABLE_SEQSCAN=off
WORK_MEM=64MB

# - Data generation parameters
RANDOM_SEED=20260901
CLUSTERS=100
QUERY_RANDOM_SEED=20260902

# - Database connection parameters
DB_HOST=127.0.0.1
DB_PORT=6543
DB_USER=linnaid
DB_NAME=pgvector_bench
